# 后端 API 地址（前端所有请求都走它）
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# 是否使用 mock 数据（true: 前端内置假数据；false: 走真后端）
NEXT_PUBLIC_USE_MOCK=true

# mock 模式下的总积分（演示用，默认 0 → 第一次发送就弹充值弹窗）
NEXT_PUBLIC_MOCK_CREDITS=0
